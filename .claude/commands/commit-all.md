# /commit-all — 全変更をコミット

## 説明

ステージングされていない変更を含む全変更をコミットする。

## 手順

1. `git add -A` — 全変更をステージング
2. 個人情報チェック（scripts/pii-check.sh）
3. コミットメッセージを入力
4. `git commit -m "<message>"`

## 注意

- 個人情報が含まれていないことを確認してからコミットする
- コミットメッセージも個人情報チェックの対象
