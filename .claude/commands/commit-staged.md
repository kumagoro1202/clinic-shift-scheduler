# /commit-staged — ステージング済みの変更をコミット

## 説明

`git add` でステージングされた変更のみをコミットする。

## 手順

1. 現在のステージング状態を確認: `git status`
2. 個人情報チェック（scripts/pii-check.sh）
3. コミットメッセージを入力
4. `git commit -m "<message>"`

## 注意

- ステージングされていない変更はコミットしない
- コミットメッセージも個人情報チェックの対象
